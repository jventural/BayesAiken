% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aiken_table.R
\name{aiken_bayes_table}
\alias{aiken_bayes_table}
\title{Analisis bayesiano completo de una tabla de calificaciones de jueces}
\usage{
aiken_bayes_table(
  data,
  judge_cols = NULL,
  id_cols = NULL,
  coefs = c("V", "H", "C"),
  local = TRUE,
  item_col = NULL,
  l = 0,
  s = 3,
  prior = c("perks", "jeffreys", "uniform"),
  alpha0 = NULL,
  cred_level = 0.95,
  thresholds = c(0.7, 0.8),
  ci_type = c("HDI", "ETI"),
  B = 10000,
  seed = NULL,
  verbose = TRUE
)
}
\arguments{
\item{data}{Data.frame con columnas identificadoras y columnas de jueces.}

\item{judge_cols}{Columnas de jueces. Si es \code{NULL} se detectan solas.}

\item{id_cols}{Columnas identificadoras. Si es \code{NULL} se detectan solas.}

\item{coefs}{Coeficientes a calcular por fila. Default \code{c("V","H","C")}.}

\item{local}{Si \code{TRUE} (default) calcula ademas A por juez-fila e I por
item-juez.}

\item{item_col}{Nombre de la columna de item, necesaria para I cuando hay
varias filas por item. Si es \code{NULL} se usa la primera de \code{id_cols}.}

\item{l, s}{Minimo y maximo de la escala.}

\item{prior, alpha0, cred_level, thresholds, ci_type, B}{Ver \code{\link{aiken_bayes}}.}

\item{seed}{Semilla para reproducibilidad.}

\item{verbose}{Mostrar el progreso y el resumen final.}
}
\value{
Objeto de clase \code{aiken_table} con los elementos \code{global}
  (una fila por unidad evaluada), \code{acuerdo} (coeficiente A) y
  \code{coherencia} (coeficiente I).
}
\description{
Calcula V, H y C por fila (item o combinacion item-criterio) y, opcionalmente,
A por juez y I por item, todo bajo el modelo Dirichlet-Multinomial.
}
\examples{
d <- data.frame(
  Item = paste0("It", rep(1:3, each = 2)),
  Criterio = rep(c("Relevancia", "Claridad"), 3),
  J1 = c(3,3,2,3,3,3), J2 = c(3,2,3,3,2,3),
  J3 = c(2,3,3,2,3,3), J4 = c(3,3,3,3,3,2)
)
res <- aiken_bayes_table(d, seed = 1, verbose = FALSE)
res$global

}
