% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bayes_aiken.R
\name{bayes_aiken}
\alias{bayes_aiken}
\title{=============================================================================
BAYES AIKEN - FUNCION PRINCIPAL
Calcula todos los coeficientes de validez de contenido con enfoque bayesiano}
\usage{
bayes_aiken(
  data,
  coefficient = "V",
  l = 0,
  s = 3,
  c = 4,
  ratings_t2 = NULL,
  ratings_others = NULL,
  ratings_item2 = NULL,
  item_col = NULL,
  criterion_col = NULL,
  prior_alpha = 1,
  prior_beta = 1,
  cred_level = 0.95,
  verbose = TRUE,
  plot = FALSE
)
}
\arguments{
\item{data}{Vector de calificaciones o data.frame con multiples items/criterios}

\item{coefficient}{Coeficiente(s) a calcular: "V", "H", "R", "C", "A", "I" o "all"}

\item{l}{Valor minimo de la escala (default = 0)}

\item{s}{Valor maximo de la escala (default = 3)}

\item{c}{Numero de categorias en la escala (default = 4, para H, R, A, I)}

\item{ratings_t2}{Segundo vector de ratings para coeficiente R (reproducibilidad)}

\item{ratings_others}{Matriz de ratings de otros jueces para coeficiente A}

\item{ratings_item2}{Ratings del segundo item para coeficiente I}

\item{item_col}{Nombre de columna de items (si data es data.frame)}

\item{criterion_col}{Nombre de columna de criterios (si data es data.frame)}

\item{prior_alpha}{Parametro alpha del prior Beta (default = 1, uniforme)}

\item{prior_beta}{Parametro beta del prior Beta (default = 1, uniforme)}

\item{cred_level}{Nivel de credibilidad (default = 0.95)}

\item{verbose}{Mostrar resultados detallados (default = TRUE)}

\item{plot}{Generar graficos (default = FALSE)}
}
\value{
Objeto de clase 'bayes_aiken' con todos los resultados
}
\description{
=============================================================================
BAYES AIKEN - FUNCION PRINCIPAL
Calcula todos los coeficientes de validez de contenido con enfoque bayesiano
}
\details{
## Coeficientes disponibles:
- **V**: V de Aiken - Validez de contenido
- **H**: Homogeneidad entre jueces
- **R**: Reproducibilidad temporal (test-retest)
- **C**: Consenso (basado en varianza)
- **A**: Acuerdo de un juez vs los demas
- **I**: Consistencia inter-item

## Modelo Bayesiano:
- Prior: Beta(alpha, beta), default uniforme Beta(1,1)
- Posterior: Beta(alpha + exitos, beta + fracasos)
- Intervalos: HDI (Highest Density Interval) y ETI (Equal-Tailed Interval)

## Tipos de Prior:
- Beta(1, 1): Uniforme (no informativo)
- Beta(0.5, 0.5): Jeffreys
- Beta(2, 2): Debilmente informativo, centrado en 0.5
}
\examples{
# Ejemplo 1: Vector simple - Coeficiente V
ratings <- c(3, 3, 2, 3, 3, 2, 3, 3, 2, 3)
result <- bayes_aiken(ratings, coefficient = "V", l = 0, s = 3)
print(result)
plot(result)

# Ejemplo 2: Multiples coeficientes
ratings <- c(2, 2, 3, 2, 2)
result <- bayes_aiken(ratings, coefficient = c("V", "H", "C"), l = 0, s = 3, c = 4)

# Ejemplo 3: Data.frame con multiples items
data <- data.frame(
  item = rep(1:3, each = 3),
  criterio = rep(c("relevancia", "representatividad", "claridad"), 3),
  J1 = c(3,3,3, 2,3,3, 3,2,3),
  J2 = c(3,3,2, 3,3,3, 3,3,2),
  J3 = c(2,3,3, 3,2,3, 2,3,3),
  J4 = c(3,3,3, 3,3,2, 3,3,3),
  J5 = c(3,2,3, 2,3,3, 3,2,3)
)
result <- bayes_aiken(data, coefficient = "V",
                      item_col = "item", criterion_col = "criterio",
                      l = 0, s = 3)

}
\author{
Jose Ventura-Leon
=============================================================================
Calcular coeficientes de validez de contenido con intervalos bayesianos

Funcion principal que calcula uno o mas coeficientes de validez de contenido
(V, H, R, C, A, I) utilizando un enfoque bayesiano con intervalos de credibilidad.
Acepta tanto vectores como data.frames.
}
