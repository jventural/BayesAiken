% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aiken_apa.R
\name{aiken_apa}
\alias{aiken_apa}
\title{Tabla de resultados en formato APA compacto}
\usage{
aiken_apa(
  x,
  umbral = NULL,
  clasico = TRUE,
  sep = "\\n",
  digits = 2,
  html = FALSE,
  pg = FALSE
)
}
\arguments{
\item{x}{Objeto \code{aiken_table}, o su elemento \code{global}.}

\item{umbral}{Umbral cuya probabilidad se muestra. Si es \code{NULL} se usa
el primero disponible.}

\item{clasico}{Si \code{TRUE} (default) anade la linea con el valor clasico.}

\item{sep}{Separador entre lineas dentro de la celda. \code{"\\n"} para
consola y Excel; \code{"<br>"} para HTML.}

\item{digits}{Decimales (default 2).}

\item{html}{Si \code{TRUE}, envuelve cada linea en \code{<span>} con clase
(\code{ab-val}, \code{ab-p}, \code{ab-clas}, \code{ab-pg}) para poder darle
estilo. Fuerza \code{sep = "<br>"}.}

\item{pg}{Si \code{TRUE}, anade a la celda de la V el intervalo frecuentista
de Penfield-Giacobbi, como referencia comparativa. Ver
\code{\link{aiken_V_ic_pg}}.}
}
\value{
Un \code{data.frame} con las columnas identificadoras y una columna
  por coeficiente.
}
\description{
Convierte la salida de \code{\link{aiken_bayes_table}} en una tabla de pocas
columnas, con un coeficiente por celda.
}
\examples{
d <- data.frame(Item = c("I1", "I2"), J1 = c(3, 2), J2 = c(3, 3),
                J3 = c(2, 3), J4 = c(3, 2))
r <- aiken_bayes_table(d, seed = 1, B = 500, local = FALSE, verbose = FALSE)
aiken_apa(r)

}
