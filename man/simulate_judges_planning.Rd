% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/advanced.R
\name{simulate_judges_planning}
\alias{simulate_judges_planning}
\title{Simulacion para planificacion del numero de jueces}
\usage{
simulate_judges_planning(
  true_V = c(0.8, 0.9),
  n_judges = c(5, 10, 15),
  k = 3,
  prior_alpha = 1,
  prior_beta = 1,
  cred_level = 0.95,
  threshold = 0.7,
  min_prob = 0.95,
  n_sim = 1000,
  seed = 123,
  verbose = TRUE
)
}
\arguments{
\item{true_V}{Vector de valores verdaderos de V a simular (default = c(0.80, 0.90))}

\item{n_judges}{Vector de numeros de jueces a evaluar (default = c(5, 10, 15))}

\item{k}{Amplitud de la escala (s - l). Para escala 0-3, k = 3 (default)}

\item{prior_alpha}{Parametro alpha del prior Beta (default = 1)}

\item{prior_beta}{Parametro beta del prior Beta (default = 1)}

\item{cred_level}{Nivel de credibilidad para intervalos (default = 0.95)}

\item{threshold}{Umbral de validez para la regla decisional (default = 0.70)}

\item{min_prob}{Probabilidad minima para la regla (default = 0.95)}

\item{n_sim}{Numero de simulaciones Monte Carlo (default = 1000)}

\item{seed}{Semilla para reproducibilidad (default = 123)}

\item{verbose}{Mostrar resultados (default = TRUE)}
}
\value{
Objeto de clase 'bayes_simulation' con:
\itemize{
  \item results: Data.frame con resultados por V verdadero y n de jueces
  \item parameters: Lista con parametros de la simulacion
  \item raw_data: Lista con datos crudos de cada simulacion
}
}
\description{
Realiza una simulacion Monte Carlo para evaluar como cambia la precision
de los intervalos y la probabilidad de cumplir una regla decisional
al variar el numero de jueces. Replica la metodologia de la Tabla 3
del articulo "V de Aiken Bayesiana".
}
\details{
Para cada combinacion de V verdadero y numero de jueces, la funcion:
\enumerate{
  \item Genera n_sim conjuntos de calificaciones simuladas
  \item Calcula la posterior Beta para cada simulacion
  \item Computa ETI y HDI al nivel cred_level
  \item Evalua cobertura (si el intervalo contiene V verdadero)
  \item Evalua si se cumple la regla P(V > threshold) >= min_prob
}

Las metricas reportadas son:
\itemize{
  \item Cobertura ETI/HDI: Proporcion de simulaciones donde el intervalo contiene V verdadero
  \item Ancho ETI/HDI: Ancho promedio del intervalo de credibilidad
  \item Pr(regla): Proporcion de simulaciones que cumplen la regla decisional
}
}
\examples{
# Version breve (pocas simulaciones)
sim <- simulate_judges_planning(true_V = 0.80, n_judges = c(5, 10),
                                k = 3, n_sim = 100)
print(sim)

\donttest{
# Replicar Tabla 3 del articulo
sim <- simulate_judges_planning(
  true_V = c(0.80, 0.90),
  n_judges = c(5, 10, 15),
  k = 3,
  n_sim = 1000
)
plot(sim)
}

}
