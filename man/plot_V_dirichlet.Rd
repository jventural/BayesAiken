% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_V_dirichlet}
\alias{plot_V_dirichlet}
\title{Graficar densidad posterior del modelo Dirichlet-Multinomial}
\usage{
plot_V_dirichlet(
  ratings,
  l = 0,
  s = 3,
  prior_alpha = 1,
  cred_level = 0.95,
  n_samples = 10000,
  threshold = 0.7,
  main = NULL,
  col = "coral",
  show_prior = FALSE,
  show_hdi = TRUE,
  seed = 42
)
}
\arguments{
\item{ratings}{Vector de calificaciones de los jueces}

\item{l}{Valor minimo de la escala (default = 0)}

\item{s}{Valor maximo de la escala (default = 3)}

\item{prior_alpha}{Parametro alpha del prior Dirichlet (default = 1, uniforme)}

\item{cred_level}{Nivel de credibilidad (default = 0.95)}

\item{n_samples}{Numero de muestras Monte Carlo (default = 10000)}

\item{threshold}{Umbral de referencia (default = 0.70)}

\item{main}{Titulo del grafico (opcional)}

\item{col}{Color de la densidad (default = "coral")}

\item{show_prior}{Mostrar distribucion prior (default = FALSE)}

\item{show_hdi}{Mostrar region HDI sombreada (default = TRUE)}

\item{seed}{Semilla para reproducibilidad (default = NULL)}
}
\value{
Invisible: lista con parametros y muestras de la posterior
}
\description{
Genera un grafico de la distribucion posterior de V usando el modelo
Dirichlet-Multinomial (mas conservador que Beta-Binomial).
}
\details{
El modelo Dirichlet-Multinomial modela directamente las frecuencias
de cada categoria, produciendo intervalos mas amplios y conservadores
que el modelo Beta-Binomial, especialmente con muestras pequenas.
}
\examples{
ratings <- c(3, 3, 2, 3, 3)
plot_V_dirichlet(ratings, l = 0, s = 3)

}
